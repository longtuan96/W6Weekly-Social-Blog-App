const initialState = {};

const blogReducer = (state = initialState, action) => {
  switch (action.type) {
    case "ADDLOG":
      break;

    default:
      break;
  }

  return { ...state };
};

export default blogReducer;
